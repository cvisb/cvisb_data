import { FilterOldDataPipe } from './filter-old-data.pipe';

describe('FilterOldDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOldDataPipe();
    expect(pipe).toBeTruthy();
  });
});
