import { ExperimentObjectPipe } from './experiment-object.pipe';

describe('ExperimentObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new ExperimentObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
