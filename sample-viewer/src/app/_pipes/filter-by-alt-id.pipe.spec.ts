import { FilterByAltIDPipe } from './filter-by-alt-id.pipe';

describe('FilterByAltIDPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByAltIDPipe();
    expect(pipe).toBeTruthy();
  });
});
