import { FilterLocusPipe } from './filter-locus.pipe';

describe('FilterLocusPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterLocusPipe();
    expect(pipe).toBeTruthy();
  });
});
