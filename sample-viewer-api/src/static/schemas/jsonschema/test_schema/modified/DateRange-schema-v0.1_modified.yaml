- '@id': cvisb:DateRange
  '@type': rdfs:Class
  rdfs:comment: A derivation of the schema.org Date class, for explicit bounded or
    unbounded date ranges. Used in json validation.
  rdfs:label: DateRange
  rdfs:subClassOf:
    '@id': schema:Date
  schema:isPartOf:
    '@id': https://schema.cvisb.org
  schema:sameAs:
    '@id': schema:Date
  validation:
    $schema: http://json-schema.org/draft-04/schema#
    properties:
      gt:
        oneOf:
        - type: string
        - type: 'null'
      gte:
        oneOf:
        - type: string
        - type: 'null'
      lt:
        oneOf:
        - type: string
        - type: 'null'
      lte:
        oneOf:
        - type: string
        - type: 'null'
    type: object
- '@id': cvisb:lte
  '@type': rdf:Property
  authenticated: false
  marginality: recommended
  owl:cardinality: one
  rdfs:comment: Inclusive upper bound of a date range
  rdfs:label: lte
  schema:domainIncludes:
  - '@id': cvisb:DateRange
  schema:rangeIncludes:
  - '@id': xsd:string
  - '@id': schema:Text
- '@id': cvisb:lt
  '@type': rdf:Property
  authenticated: false
  marginality: recommended
  owl:cardinality: one
  rdfs:comment: Exclusive upper bound of a date range
  rdfs:label: lt
  schema:domainIncludes:
  - '@id': cvisb:DateRange
  schema:rangeIncludes:
  - '@id': xsd:string
  - '@id': schema:Text
- '@id': cvisb:gte
  '@type': rdf:Property
  authenticated: false
  marginality: recommended
  owl:cardinality: one
  rdfs:comment: Inclusive lower bound of a date range
  rdfs:label: gte
  schema:domainIncludes:
  - '@id': cvisb:DateRange
  schema:rangeIncludes:
  - '@id': xsd:string
  - '@id': schema:Text
- '@id': cvisb:gt
  '@type': rdf:Property
  authenticated: false
  marginality: recommended
  owl:cardinality: one
  rdfs:comment: Exclusive lower bound of a date range
  rdfs:label: gt
  schema:domainIncludes:
  - '@id': cvisb:DateRange
  schema:rangeIncludes:
  - '@id': xsd:string
  - '@id': schema:Text
